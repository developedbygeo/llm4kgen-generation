export const logBigMessage = (message: string): void => {
    console.log(
        '--------------------------------------------------------------------\n' +
            `${message}\n` +
            '--------------------------------------------------------------------'
    );
};
